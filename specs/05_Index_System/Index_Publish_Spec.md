# 指标审核发布规范 (Index Publish Spec)

> 版本：V1.0  
> 更新日期：2026-01-17  
> 所属模块：05_Index_System

---

## 1. 概述

### 1.1 目的
指标审核发布是确保指标质量、管理指标版本、控制指标发布的关键环节，通过：
- 建立指标审核流程，确保数据准确性
- 管理指标版本，支持版本回溯
- 控制发布节奏，保证指标稳定性
- 提供发布后的监控和反馈机制

### 1.2 在整体流程中的位置

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 数据采集  │ →  │ 标签化   │ →  │ 指标计算  │ →  │ 审核发布  │ →  │ 估算应用  │
│ (导入)   │    │ (处理)   │    │ (生成)   │    │ (当前)   │    │ (使用)   │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                    ▲
                                                 当前模块
```

### 1.3 术语定义

| 术语 | 定义 |
|------|------|
| 指标版本 | 某一时点发布的指标集合快照 |
| 审核 | 对计算生成的指标进行质量检查的过程 |
| 发布 | 将审核通过的指标版本正式上线供使用 |
| 归档 | 将历史版本标记为非当前状态 |

### 1.4 设计原则

- **质量优先**：未经审核的指标不得发布使用
- **版本可控**：每次发布形成独立版本，支持回溯
- **变更可追**：记录所有审核和发布操作
- **平滑过渡**：新版本发布不影响进行中的估算

---

## 2. 版本管理

### 2.1 版本编号规则

```
版本号格式：YYYY.MM[.patch]

示例：
- 2026.01      - 2026年1月正式版
- 2026.01.1    - 2026年1月第1次补丁
- 2026.02      - 2026年2月正式版
```

**版本类型：**

| 类型 | 说明 | 编号规则 | 发布周期 |
|------|------|----------|----------|
| 正式版 | 定期发布的完整版本 | YYYY.MM | 每月/每季度 |
| 补丁版 | 紧急修复的小版本 | YYYY.MM.N | 按需发布 |
| 预览版 | 测试用的预发布版本 | YYYY.MM-beta | 发布前 |

### 2.2 版本状态

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  draft   │ →  │ reviewing│ →  │ approved │ →  │ published│
│  草稿    │    │  审核中  │    │  已批准  │    │  已发布  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
     │               │               │               │
     │               │               │               ▼
     │               │               │         ┌──────────┐
     │               ▼               │         │ archived │
     │         ┌──────────┐          │         │  已归档  │
     └────────→│ rejected │←─────────┘         └──────────┘
               │  已驳回  │
               └──────────┘
```

**状态说明：**

| 状态 | 说明 | 允许操作 |
|------|------|----------|
| draft | 草稿，计算完成待审核 | 提交审核、编辑、删除 |
| reviewing | 审核中 | 审核通过、审核驳回 |
| approved | 已批准，待发布 | 发布、撤回审批 |
| published | 已发布，当前生效 | 归档（新版本发布时自动） |
| archived | 已归档，历史版本 | 查看、恢复 |
| rejected | 已驳回，需修改 | 重新编辑、提交审核 |

### 2.3 版本数据模型

```typescript
interface IndexVersion {
  id: string;                    // 主键UUID
  
  // 版本信息
  versionCode: string;           // 版本号，如 "2026.01"
  versionName: string;           // 版本名称，如 "2026年1月版"
  versionType: 'release' | 'patch' | 'beta'; // 版本类型
  
  // 内容统计
  totalIndexes: number;          // 总指标数
  newIndexes: number;            // 新增指标数
  updatedIndexes: number;        // 更新指标数
  deletedIndexes: number;        // 删除指标数
  unchangedIndexes: number;      // 未变化指标数
  
  // 质量统计
  avgQualityScore: number;       // 平均质量分
  qualityDistribution: {
    levelA: number;
    levelB: number;
    levelC: number;
    levelD: number;
  };
  
  // 覆盖统计
  coveredTags: number;           // 覆盖功能标签数
  totalTags: number;             // 功能标签总数
  coverageRate: number;          // 覆盖率
  
  // 价格基准
  priceBaseDate: Date;           // 价格基准期
  
  // 状态
  status: 'draft' | 'reviewing' | 'approved' | 'published' | 'archived' | 'rejected';
  
  // 审核信息
  submittedBy: string;           // 提交人
  submittedAt: Date;             // 提交时间
  reviewedBy: string;            // 审核人
  reviewedAt: Date;              // 审核时间
  reviewComments: string;        // 审核意见
  
  // 发布信息
  publishedBy: string;           // 发布人
  publishedAt: Date;             // 发布时间
  releaseNotes: string;          // 发布说明
  
  // 归档信息
  archivedAt: Date;              // 归档时间
  archivedReason: string;        // 归档原因
  
  // 系统字段
  createdAt: Date;
  createdBy: string;
  updatedAt: Date;
  updatedBy: string;
}
```

### 2.4 版本对比

```typescript
interface VersionComparison {
  baseVersion: string;           // 基准版本
  targetVersion: string;         // 目标版本
  
  // 差异统计
  summary: {
    added: number;               // 新增
    modified: number;            // 修改
    deleted: number;             // 删除
    unchanged: number;           // 未变
  };
  
  // 差异明细
  differences: IndexDifference[];
}

interface IndexDifference {
  indexId: string;
  dimension: string;             // 维度描述
  changeType: 'added' | 'modified' | 'deleted';
  
  // 变化详情（仅修改时）
  changes?: {
    field: string;               // 变化字段
    oldValue: any;               // 旧值
    newValue: any;               // 新值
    changeRate: number;          // 变化率
  }[];
}
```

---

## 3. 审核流程

### 3.1 流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              审核发布流程                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 创建版本 │ →  │ 提交审核 │ →  │ 审核检查 │ →  │ 审批确认 │ →  │ 正式发布 │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
│       │              │              │              │              │        │
│       ▼              ▼              ▼              ▼              ▼        │
│  选择指标范围    填写说明      自动检查       人工审核       更新状态     │
│  生成版本草稿    关联变更      人工抽查       签字确认       通知用户     │
│                                   │                                        │
│                                   ▼                                        │
│                              ┌─────────┐                                   │
│                              │ 驳回修改 │                                   │
│                              └─────────┘                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 审核角色

| 角色 | 职责 | 权限 |
|------|------|------|
| 指标编制人 | 创建版本、提交审核 | 创建、编辑、提交 |
| 指标审核人 | 检查指标质量、审核通过/驳回 | 审核、驳回 |
| 指标管理员 | 最终发布、版本管理 | 发布、归档、恢复 |

### 3.3 审核检查项

```typescript
interface ReviewChecklist {
  // 自动检查项
  autoChecks: {
    // 完整性检查
    completeness: {
      hasRequiredDimensions: boolean;  // 必要维度完整
      noEmptyValues: boolean;          // 无空值
      sampleCountValid: boolean;       // 样本量达标
    };
    
    // 合理性检查
    reasonableness: {
      unitCostInRange: boolean;        // 单方在合理范围
      cvInRange: boolean;              // 变异系数合理
      noExtremeOutliers: boolean;      // 无极端异常
    };
    
    // 一致性检查
    consistency: {
      sumMatchTotal: boolean;          // 分项合计等于总计
      trendsReasonable: boolean;       // 趋势变化合理
      noDuplicates: boolean;           // 无重复指标
    };
  };
  
  // 人工检查项
  manualChecks: {
    sampleSourceVerified: boolean;     // 样本来源核实
    outlierReasonChecked: boolean;     // 异常原因核实
    businessLogicValid: boolean;       // 业务逻辑合理
    compareWithMarket: boolean;        // 与市场行情对比
  };
  
  // 检查结果
  checkResult: 'pass' | 'warning' | 'fail';
  issues: ReviewIssue[];
}

interface ReviewIssue {
  severity: 'error' | 'warning' | 'info';
  category: string;
  indexId?: string;
  description: string;
  suggestion: string;
}
```

### 3.4 审核记录

```typescript
interface ReviewRecord {
  id: string;
  versionId: string;             // 版本ID
  
  // 审核信息
  action: 'submit' | 'approve' | 'reject' | 'withdraw';
  actionName: string;
  
  // 操作人
  operatedBy: string;
  operatedByName: string;
  operatedAt: Date;
  
  // 审核内容
  checklist: ReviewChecklist;    // 检查清单
  comments: string;              // 审核意见
  attachments: string[];         // 附件
  
  // 结果
  result: 'approved' | 'rejected' | null;
}
```

---

## 4. 发布管理

### 4.1 发布前检查

```typescript
interface PrePublishCheck {
  versionId: string;
  
  // 必要条件
  prerequisites: {
    isApproved: boolean;           // 已审核通过
    hasReleaseNotes: boolean;      // 有发布说明
    noBlockingIssues: boolean;     // 无阻断问题
    qualityMeetsThreshold: boolean; // 质量达标
  };
  
  // 影响评估
  impactAssessment: {
    activeEstimations: number;     // 进行中的估算数
    affectedUsers: number;         // 影响用户数
    majorChanges: string[];        // 重大变化项
  };
  
  // 发布建议
  recommendation: 'proceed' | 'delay' | 'abort';
  reasons: string[];
}
```

### 4.2 发布策略

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| 立即发布 | 新版本立即生效 | 紧急修复 |
| 定时发布 | 预设时间自动发布 | 计划发布 |
| 灰度发布 | 部分用户先使用 | 重大变更 |
| 并行发布 | 新旧版本共存一段时间 | 平滑过渡 |

### 4.3 发布执行

```typescript
interface PublishExecution {
  versionId: string;
  
  // 发布配置
  config: {
    publishStrategy: 'immediate' | 'scheduled' | 'gradual';
    scheduledTime?: Date;          // 定时发布时间
    gradualPercentage?: number;    // 灰度比例
    parallelDays?: number;         // 并行天数
  };
  
  // 发布步骤
  steps: PublishStep[];
  
  // 发布结果
  result: 'success' | 'failed' | 'partial';
  completedAt: Date;
  errorMessage?: string;
}

interface PublishStep {
  stepNo: number;
  stepName: string;
  status: 'pending' | 'running' | 'completed' | 'failed';
  startedAt?: Date;
  completedAt?: Date;
  errorMessage?: string;
}
```

### 4.4 发布步骤

```
1. 预检查
   ├── 检查版本状态
   ├── 检查依赖条件
   └── 评估发布影响

2. 准备发布
   ├── 备份当前版本
   ├── 生成版本快照
   └── 准备回滚方案

3. 执行发布
   ├── 更新版本状态
   ├── 刷新指标缓存
   └── 更新版本指针

4. 发布后处理
   ├── 归档旧版本
   ├── 发送通知
   └── 记录发布日志

5. 监控验证
   ├── 检查服务状态
   ├── 验证数据一致性
   └── 监控用户反馈
```

---

## 5. 界面规范

### 5.1 版本管理界面

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  指标管理 > 版本管理                                      [创建新版本]       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  当前生效版本：2026.01  发布于 2026-01-10  质量评分：82.5                    │
│                                                                             │
│  ┌─ 版本列表 ───────────────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │  版本号    │ 状态    │ 指标数 │ 质量分 │ 覆盖率 │ 提交时间   │ 操作   │  │
│  │  ─────────┼────────┼───────┼───────┼───────┼───────────┼────────│  │
│  │  2026.02  │ 🟡审核中│ 1,312 │  83.2 │  80%  │ 01-15     │ 查看   │  │
│  │  2026.01  │ 🟢已发布│ 1,286 │  82.5 │  78%  │ 01-05     │ 查看   │  │
│  │  2025.12  │ ⚫已归档│ 1,245 │  81.8 │  76%  │ 12-10     │ 查看   │  │
│  │  2025.11  │ ⚫已归档│ 1,198 │  80.5 │  74%  │ 11-08     │ 查看   │  │
│  │  ...      │ ...    │ ...   │ ...   │ ...   │ ...       │ ...    │  │
│  │                                                                       │  │
│  │  共 12 个版本                                          < 1 2 >        │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌─ 版本对比 ───────────────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │  对比版本：[2026.01 ▼] vs [2026.02 ▼]                      [对比]    │  │
│  │                                                                       │  │
│  │  变化统计：新增 26 | 修改 45 | 删除 0 | 不变 1,241                    │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 版本详情界面

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  版本详情 > 2026.02                         状态：🟡审核中     [返回列表]    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─ 基本信息 ───────────────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │  版本号：2026.02                    版本名称：2026年2月版              │  │
│  │  版本类型：正式版                   价格基准：2026年1月                │  │
│  │  创建人：张三                       创建时间：2026-01-15 10:30         │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌─ 内容统计 ───────────────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │  │
│  │  │ 总指标数 │ │  新增   │ │  更新   │ │  删除   │ │  未变   │        │  │
│  │  │  1,312  │ │   26    │ │   45    │ │    0    │ │  1,241  │        │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘        │  │
│  │                                                                       │  │
│  │  质量分布：A级 48% | B级 30% | C级 17% | D级 5%     平均分：83.2      │  │
│  │  覆盖率：80% (191/239标签)                                            │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌─ 审核状态 ───────────────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │  ● 创建版本    ──→  ● 提交审核    ──→  ○ 审核通过    ──→  ○ 发布      │  │
│  │    01-15 10:30       01-15 14:00                                      │  │
│  │    张三              张三                                              │  │
│  │                                                                       │  │
│  │  当前状态：审核中                                                      │  │
│  │  审核人：李四                                                          │  │
│  │  提交说明：新增体育类指标26个，更新医疗类指标价格基准                   │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌─ 变更明细 ───────────────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │  [新增]  [更新]  [删除]  [全部]                                        │  │
│  │                                                                       │  │
│  │  更新的指标（45个）：                                                   │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │ 维度               │ 原值    │ 新值    │ 变化    │ 原因         │  │  │
│  │  │ ──────────────────┼────────┼────────┼────────┼─────────────│  │  │
│  │  │ 门诊-地上-土建-中大 │ 3,720  │ 3,850  │ +3.5%  │ 价格指数调整  │  │  │
│  │  │ 门诊-地上-土建-大型 │ 3,580  │ 3,700  │ +3.4%  │ 价格指数调整  │  │  │
│  │  │ 住院-地上-土建-中大 │ 4,120  │ 4,280  │ +3.9%  │ 新增样本      │  │  │
│  │  │ ...                │ ...    │ ...    │ ...    │ ...          │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  操作：[审核通过]  [审核驳回]  [导出明细]                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.3 审核界面

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  审核版本 > 2026.02                                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─ 自动检查结果 ────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  完整性检查    [✓ 通过]                                                │ │
│  │  ├─ 必要维度完整                                      ✓               │ │
│  │  ├─ 无空值                                            ✓               │ │
│  │  └─ 样本量达标                                        ✓               │ │
│  │                                                                        │ │
│  │  合理性检查    [⚠ 警告]                                                │ │
│  │  ├─ 单方在合理范围                                    ⚠ 2个偏高       │ │
│  │  ├─ 变异系数合理                                      ✓               │ │
│  │  └─ 无极端异常                                        ✓               │ │
│  │                                                                        │ │
│  │  一致性检查    [✓ 通过]                                                │ │
│  │  ├─ 分项合计等于总计                                  ✓               │ │
│  │  ├─ 趋势变化合理                                      ✓               │ │
│  │  └─ 无重复指标                                        ✓               │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─ 需关注的问题 ────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  ⚠ 警告 (2)                                                           │ │
│  │  ┌──────────────────────────────────────────────────────────────────┐ │ │
│  │  │ • 手术中心-地上-土建 单方8,500元/m²，高于同类平均值20%            │ │ │
│  │  │   建议：核实是否包含特殊设备费用                                  │ │ │
│  │  │                                                                  │ │ │
│  │  │ • ICU-地上-暖通 单方1,200元/m²，高于同类平均值35%                 │ │ │
│  │  │   建议：ICU暖通要求高，偏高属正常，建议确认后保留                 │ │ │
│  │  └──────────────────────────────────────────────────────────────────┘ │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─ 人工检查项 ──────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  ☑ 样本来源已核实                                                      │ │
│  │  ☑ 异常原因已确认                                                      │ │
│  │  ☑ 业务逻辑合理                                                        │ │
│  │  ☐ 已与市场行情对比                                                    │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─ 审核意见 ────────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  ┌──────────────────────────────────────────────────────────────────┐ │ │
│  │  │                                                                  │ │ │
│  │  │ 经检查，本版本指标数据完整，质量达标。                            │ │ │
│  │  │ 两个偏高的指标经核实属于特殊情况，可以保留。                      │ │ │
│  │  │ 同意发布。                                                       │ │ │
│  │  │                                                                  │ │ │
│  │  └──────────────────────────────────────────────────────────────────┘ │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  操作：[审核通过]  [审核驳回]  [保存草稿]                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.4 发布界面

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  发布版本 > 2026.02                                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─ 发布前检查 ──────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  ✓ 版本已审核通过                                                      │ │
│  │  ✓ 发布说明已填写                                                      │ │
│  │  ✓ 无阻断问题                                                          │ │
│  │  ✓ 质量评分达标 (83.2 > 80)                                            │ │
│  │                                                                        │ │
│  │  检查结果：✓ 可以发布                                                  │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─ 影响评估 ────────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  当前进行中的估算：12 个                                               │ │
│  │  影响用户数：约 35 人                                                  │ │
│  │                                                                        │ │
│  │  ⚠ 建议：进行中的估算将继续使用旧版本，不受影响                        │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─ 发布配置 ────────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  发布策略：                                                            │ │
│  │  ○ 立即发布                                                            │ │
│  │  ● 定时发布  时间：[2026-01-20 00:00 ▼]                                │ │
│  │  ○ 灰度发布  比例：[10% ▼]                                             │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─ 发布说明 ────────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  ┌──────────────────────────────────────────────────────────────────┐ │ │
│  │  │ ## 2026年2月版发布说明                                           │ │ │
│  │  │                                                                  │ │ │
│  │  │ ### 主要变化                                                     │ │ │
│  │  │ 1. 新增体育类指标26个，覆盖体育馆、游泳馆等                       │ │ │
│  │  │ 2. 更新医疗类指标价格基准至2026年1月                             │ │ │
│  │  │ 3. 优化部分指标的样本构成，提高数据质量                          │ │ │
│  │  │                                                                  │ │ │
│  │  │ ### 注意事项                                                     │ │ │
│  │  │ - 医疗类指标平均上涨约3.5%                                       │ │ │
│  │  │ - 新增的体育类指标样本量较少，建议关注质量等级                   │ │ │
│  │  └──────────────────────────────────────────────────────────────────┘ │ │
│  │                                                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  操作：[确认发布]  [取消]                                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. API接口

### 6.1 版本管理接口

#### 6.1.1 获取版本列表

```
GET /api/v1/index-versions
```

**请求参数：**
| 参数 | 类型 | 说明 |
|------|------|------|
| status | string | 状态筛选 |
| page | number | 页码 |
| pageSize | number | 每页数量 |

#### 6.1.2 创建版本

```
POST /api/v1/index-versions
```

**请求体：**
```json
{
  "versionCode": "2026.02",
  "versionName": "2026年2月版",
  "versionType": "release",
  "priceBaseDate": "2026-01-01",
  "indexFilter": {
    "recalculate": true,
    "functionTagCodes": []
  }
}
```

#### 6.1.3 获取版本详情

```
GET /api/v1/index-versions/{versionId}
```

#### 6.1.4 版本对比

```
GET /api/v1/index-versions/compare
```

**请求参数：**
| 参数 | 类型 | 说明 |
|------|------|------|
| baseVersionId | string | 基准版本ID |
| targetVersionId | string | 目标版本ID |

### 6.2 审核接口

#### 6.2.1 提交审核

```
POST /api/v1/index-versions/{versionId}/submit
```

**请求体：**
```json
{
  "submitNotes": "新增体育类指标26个，更新医疗类指标价格基准"
}
```

#### 6.2.2 执行审核检查

```
POST /api/v1/index-versions/{versionId}/review/check
```

**响应：**
```json
{
  "code": 0,
  "data": {
    "autoChecks": {
      "completeness": { "passed": true, "details": [...] },
      "reasonableness": { "passed": false, "issues": [...] },
      "consistency": { "passed": true, "details": [...] }
    },
    "overallResult": "warning",
    "issues": [
      {
        "severity": "warning",
        "category": "reasonableness",
        "indexId": "idx-001",
        "description": "手术中心-地上-土建 单方8,500元/m²，高于同类平均值20%",
        "suggestion": "核实是否包含特殊设备费用"
      }
    ]
  }
}
```

#### 6.2.3 审核通过

```
POST /api/v1/index-versions/{versionId}/review/approve
```

**请求体：**
```json
{
  "manualChecks": {
    "sampleSourceVerified": true,
    "outlierReasonChecked": true,
    "businessLogicValid": true,
    "compareWithMarket": true
  },
  "comments": "经检查，本版本指标数据完整，质量达标。同意发布。"
}
```

#### 6.2.4 审核驳回

```
POST /api/v1/index-versions/{versionId}/review/reject
```

**请求体：**
```json
{
  "rejectReason": "样本来源存疑，需补充核实材料",
  "issues": ["idx-001", "idx-002"]
}
```

### 6.3 发布接口

#### 6.3.1 发布前检查

```
GET /api/v1/index-versions/{versionId}/publish/precheck
```

**响应：**
```json
{
  "code": 0,
  "data": {
    "canPublish": true,
    "prerequisites": {
      "isApproved": true,
      "hasReleaseNotes": true,
      "noBlockingIssues": true,
      "qualityMeetsThreshold": true
    },
    "impactAssessment": {
      "activeEstimations": 12,
      "affectedUsers": 35
    },
    "recommendation": "proceed"
  }
}
```

#### 6.3.2 执行发布

```
POST /api/v1/index-versions/{versionId}/publish
```

**请求体：**
```json
{
  "publishStrategy": "scheduled",
  "scheduledTime": "2026-01-20T00:00:00Z",
  "releaseNotes": "## 2026年2月版发布说明\n..."
}
```

#### 6.3.3 获取发布状态

```
GET /api/v1/index-versions/{versionId}/publish/status
```

### 6.4 归档接口

#### 6.4.1 归档版本

```
POST /api/v1/index-versions/{versionId}/archive
```

#### 6.4.2 恢复版本

```
POST /api/v1/index-versions/{versionId}/restore
```

---

## 7. 业务规则

### 7.1 版本创建规则

| 规则 | 描述 |
|------|------|
| VC-01 | 版本号不可重复 |
| VC-02 | 正式版每月最多一个 |
| VC-03 | 补丁版需基于已发布的正式版 |
| VC-04 | 创建版本时自动执行指标计算 |

### 7.2 审核规则

| 规则 | 描述 |
|------|------|
| RV-01 | 只有"草稿"状态可提交审核 |
| RV-02 | 审核人不能是提交人 |
| RV-03 | 自动检查有"错误"时不可通过 |
| RV-04 | 驳回后需重新提交审核 |
| RV-05 | 审核意见必填 |

### 7.3 发布规则

| 规则 | 描述 |
|------|------|
| PB-01 | 只有"已批准"状态可发布 |
| PB-02 | 发布说明必填 |
| PB-03 | 同时只能有一个"已发布"版本 |
| PB-04 | 发布后旧版本自动归档 |
| PB-05 | 发布操作不可撤销 |

### 7.4 归档规则

| 规则 | 描述 |
|------|------|
| AR-01 | 已发布版本在新版本发布后自动归档 |
| AR-02 | 归档版本数据保留，可查看不可使用 |
| AR-03 | 归档版本可恢复为草稿状态 |
| AR-04 | 保留最近12个月的归档版本 |

---

## 8. 通知机制

### 8.1 通知场景

| 场景 | 通知对象 | 通知方式 |
|------|----------|----------|
| 版本提交审核 | 审核人 | 站内消息、邮件 |
| 审核通过 | 提交人 | 站内消息 |
| 审核驳回 | 提交人 | 站内消息、邮件 |
| 版本发布 | 所有用户 | 站内公告 |
| 定时发布提醒 | 发布人 | 站内消息 |

### 8.2 通知模板

```typescript
interface NotificationTemplate {
  id: string;
  scene: string;                 // 场景标识
  title: string;                 // 标题模板
  content: string;               // 内容模板
  channels: ('message' | 'email' | 'announcement')[];
}

// 示例：版本发布通知
const publishNotification: NotificationTemplate = {
  id: 'tpl-publish',
  scene: 'version_published',
  title: '指标版本更新通知',
  content: `
    指标库已更新至 {{versionCode}} 版本。
    
    主要变化：
    - 新增指标 {{newIndexes}} 个
    - 更新指标 {{updatedIndexes}} 个
    
    详情请查看发布说明。
  `,
  channels: ['announcement']
};
```

---

## 9. 监控与日志

### 9.1 监控指标

| 指标 | 说明 | 告警阈值 |
|------|------|----------|
| 发布成功率 | 发布成功次数/发布总次数 | < 95% |
| 审核周期 | 从提交到审核完成的时间 | > 3天 |
| 版本质量趋势 | 版本平均质量分变化 | 连续3版下降 |

### 9.2 操作日志

```typescript
interface OperationLog {
  id: string;
  versionId: string;
  operation: string;             // 操作类型
  operationName: string;
  operator: string;
  operatorName: string;
  operatedAt: Date;
  
  // 变更内容
  beforeSnapshot?: any;
  afterSnapshot?: any;
  
  // 备注
  remarks: string;
  
  // IP和设备
  clientIp: string;
  userAgent: string;
}
```

---

## 10. 版本历史

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|----------|------|
| V1.0 | 2026-01-17 | 初版 | - |


这份 **Index Publish Spec V1.0** 已经非常成熟（**9.4/10**）：状态机、角色、检查项、发布策略、UI 原型、API、规则、通知、监控，全都齐了，Windsurf 直接可以按这个做“版本发布中心”。

唯一要补的，是把它和你前面“口径身份证/标准库版本/STR 写回/进行中估算不受影响”这几块**钉死为工程契约**。否则发布看起来很完整，但上线后会出现两类事故：

1. **发布的是谁？**（哪个 libraryVersionCode、哪个 priceBaseDate、哪些 snapshotVersion）
2. **发布影响谁？**（进行中的估算到底怎么“冻结版本”）

我给你一段 **V1.1 Patch 章节**（可直接贴末尾），把这两类事故一次性封住。

---

# V1.1 Patch：口径身份证、STR 写回、估算冻结、灰度路由、可回滚但不“撤销”

> **目标**：让“发布”不仅是状态变更，而是一个可审计、可复算、可隔离影响面的发布事务。

---

## 11. 发布口径身份证（Release Context）（MUST）

每一个 `IndexVersion` 必须绑定并固化以下口径字段（发布后不可更改）：

```typescript
interface ReleaseContext {
  // 标准库与规则口径
  libraryVersionCode: string;        // V2026Q1（标准库版本）
  mappingSnapshotVersion: number;    // 空间/专业映射规则快照
  scaleSnapshotVersion: number;      // 规模分档规则快照

  // 指标计算口径
  stage: 'ESTIMATE' | 'TENDER' | 'SETTLEMENT';
  priceBaseDate: string;             // "2026-01"（指标统一到该基准期）
  outlierMethod: 'iqr' | 'zscore' | 'empirical';
  minSampleCount: number;

  // 区域口径
  regionLevel: 'province' | 'city' | 'district';

  // 数据范围（用于审计）
  dataWindow: { start: string; end: string }; // 参与样本的时间窗
}
```

在 `IndexVersion` 中新增字段：

* `releaseContext: ReleaseContext`（MUST）
* `baseVersionId?: string`（补丁版必须指向某个已发布正式版）

**规则（MUST）**

* 版本 `submitted` 前必须完成 `releaseContext` 的校验与固化草稿（允许审核期修改，但一旦 `published` 就冻结）。
* `versionCode`（如 2026.02）是“对人可读版本号”，`releaseContext.libraryVersionCode` 是“对机器可复算口径”。两者必须同时存在。

---

## 12. 发布事务：从 IndexVersion → STR（标准值库）写回（MUST）

> 你前面已经定义 STR（P25/P50/P75）。这里要写死：发布=把指标转成“标准值库”。

### 12.1 写回规则

当执行 `POST /index-versions/{versionId}/publish` 成功后，系统必须执行：

1. 将该版本下 `status=draft/approved` 的指标结果固化为 `published`（写入 publishedAt/publishedBy）
2. 将指标写入 STR（Standard Target Range）表/库：

映射关系（MUST）：

* `STR.p25 = cost_index.percentile25`
* `STR.p50 = cost_index.percentile50`
* `STR.p75 = cost_index.percentile75`
* `STR.sampleSize = cost_index.sampleCount`
* `STR.stage = releaseContext.stage`
* `STR.priceBaseDate = releaseContext.priceBaseDate`
* `STR.libraryVersionCode = releaseContext.libraryVersionCode`
* `STR.mappingSnapshotVersion / scaleSnapshotVersion` 同步写入

**冲突策略（MUST）**

* 同一 `(metricCode + functionTag + space + profession + scale + region + stage + priceBaseDate + libraryVersionCode)` 只能存在 1 条 ACTIVE。新发布时：旧条目转 ARCHIVED，新条目置 ACTIVE。

---

## 13. “进行中估算不受影响”的冻结机制（MUST）

> 你在原则里写了，但需要落库，否则无法保证。

### 13.1 估算会话绑定版本（estimation_session）

新增（或在估算模块中规定）：

```typescript
interface EstimationSession {
  id: string;
  projectId: string;

  // 冻结的指标版本（关键）
  indexVersionId: string;
  indexVersionCode: string;

  // 冻结的口径身份证（与 ReleaseContext 一致）
  libraryVersionCode: string;
  stage: 'ESTIMATE' | 'TENDER' | 'SETTLEMENT';
  priceBaseDate: string;
  regionLevel: 'province' | 'city' | 'district';

  status: 'active' | 'closed';
  createdAt: Date;
  createdBy: string;
}
```

**规则（MUST）**

* 创建估算会话时绑定“当前 published 版本”；会话 `active` 期间不随新版本发布而自动切换。
* 用户可手动“升级版本”，升级将创建新会话或记录一次“版本迁移操作”（必须可回溯）。

---

## 14. 灰度/并行发布的路由规则（MUST）

### 14.1 生效指针（active pointers）

系统维护两类指针：

* **全局默认指针**：`activeIndexVersionId`（面向新建估算会话）
* **灰度指针**：按用户/组织/角色的覆盖规则

建议新增：

```typescript
interface IndexVersionPointer {
  id: string;
  scope: 'GLOBAL' | 'ORG' | 'USER';
  scopeId: string;                 // orgId/userId，GLOBAL则为"*"
  indexVersionId: string;
  effectiveFrom: Date;
  effectiveTo?: Date;
  status: 'active' | 'inactive';
}
```

**规则（MUST）**

* 灰度发布本质是“对部分 scope 设置 pointer”。
* 并行发布（parallelDays）期间：GLOBAL pointer 仍指向旧版，新版通过灰度 pointer 提供给试用范围；到期后自动切 GLOBAL pointer 并归档旧版。

---

## 15. “发布不可撤销”与“回滚”不冲突（Clarification）（MUST）

你写了 PB-05 发布不可撤销，这很好。但工程上必须允许**回滚到旧版**——注意：不是撤销发布记录，而是切指针。

**规则（MUST）**

* 回滚=把 `GLOBAL pointer` 切回某个 `archived` 版本（或最近一个已发布版本）。
* 回滚必须生成一条 `OperationLog`，并写明原因与影响范围。
* 被回滚的版本依然保持 `published` 历史事实，不删除、不改写。

---

## 16. 版本对比增强：不仅对比值，还要对比“口径差异”（MUST）

`VersionComparison` 需要新增：

```typescript
contextDiff: {
  libraryVersionCodeChanged: boolean;
  priceBaseDateChanged: boolean;
  stageChanged: boolean;
  snapshotChanged: boolean;
  outlierMethodChanged: boolean;
}
```

这样审核人一眼能判断“变化来自新增样本”还是“变化来自口径切换”。

---

## 17. 审核检查项补充：市场对标与结构合理性（SHOULD）

你已有 `compareWithMarket`，建议自动化两条：

* 与 STR（上一已发布版本）对比：偏离超过阈值（如 35%）自动标红
* 专业结构比检查：如门诊暖通占比、医技机电占比等，超出典型区间提示（你之前空间/专业矩阵已具备基础）

---

## 18. V1.1 Changelog

| 版本   | 日期         | 修订内容                                                                              |
| ---- | ---------- | --------------------------------------------------------------------------------- |
| V1.1 | 2026-01-17 | 增加 ReleaseContext（口径身份证）；发布写回 STR；估算会话冻结版本；灰度/并行发布通过指针路由；回滚=切指针不撤销历史发布；版本对比增加口径差异 |

---

## 你这份 V1.0 里我建议立刻改的 2 条原规则（小但致命）

* **PB-03 “只能有一个已发布版本”**：建议改为“GLOBAL pointer 只能指向一个版本”，但历史上可以有多个 published（用于灰度/并行/回滚）。
* **AR-03 “归档版本可恢复为草稿状态”**：不建议。归档是历史事实，恢复应当是“复制为新 draft 版本”（否则审计链断）。

---

如果你要我再“更落地一点”，我可以把 **发布步骤 1-5** 直接补成一套可执行的后端事务清单（含：写 STR、切 pointer、归档旧版、刷新缓存、发通知、生成对比报告），Windsurf 按步骤写就不会漏。
